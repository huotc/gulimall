<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.htc.gulimall.product.dao.CategoryDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.htc.gulimall.product.entity.CategoryEntity" id="categoryMap">
        <result property="catId" column="cat_id"/>
        <result property="name" column="name"/>
        <result property="parentCid" column="parent_cid"/>
        <result property="catLevel" column="cat_level"/>
        <result property="showStatus" column="show_status"/>
        <result property="sort" column="sort"/>
        <result property="icon" column="icon"/>
        <result property="productUnit" column="product_unit"/>
        <result property="productCount" column="product_count"/>
    </resultMap>
    <select id="getCategoryHierarchy" resultMap="categoryMap">
        WITH RECURSIVE category_hierarchy AS (
            select cat_id
                 , name
                 , parent_cid
                 , cat_level
            from pms_category
            where cat_id = #{catId}
            union all
            select pc.cat_id
                 , pc.name
                 , pc.parent_cid
                 , pc.cat_level
            from pms_category pc
                     inner join category_hierarchy ch
                                on pc.cat_id = ch.parent_cid
        )
        SELECT cat_id, name, parent_cid, cat_level
        FROM category_hierarchy
        ORDER BY cat_level
    </select>
</mapper>